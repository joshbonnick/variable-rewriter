<?php

function calculate_totalPrice($productPrice, $discountAmount) {
    $total_price = $productPrice - $discountAmount;
    return $total_price;
}

$ProductPrice = 50;
$discount_amount = 10;

$totalPrice = calculate_totalPrice($ProductPrice, $discount_amount);

echo "Total Price after discount: $" . $totalPrice;